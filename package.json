{
  "name": "juho-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-assets.mjs && vite",
    "build": "node scripts/build-site.mjs",
    "check": "node --check dist/deck.js && node --check dist/scene.js && node --check dist/assembly.js",
    "test": "node scripts/check-deck.mjs && node scripts/check-scene.mjs"
  },
  "dependencies": {
    "gsap": "3.15.0",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@sinonjs/fake-timers": "13.0.5",
    "jsdom": "26.1.0",
    "vite": "7.1.7"
  }
}
